# SessionTraceViewer

`SessionTraceViewer` is a macOS app for opening and inspecting `ReducerArchitecture` session trace recordings.

## Development

Open `SessionTraceViewer.xcodeproj` in Xcode and run the `SessionTraceViewer` scheme.
